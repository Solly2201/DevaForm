{
  "name": "@devaform/character-schema",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned, serializable character configuration schema, skeleton and socket definitions",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(linted via app eslint)\"",
    "test": "vitest run",
    "export-canonical": "tsx scripts/export-canonical.ts"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
